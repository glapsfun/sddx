{
  "name": "sddx",
  "version": "2.0.0",
  "description": "Loop-based Spec-Driven Development: dense specs with mandatory oracles, hook-enforced TDD, parallel worktrees, hash-chained receipts.",
  "author": {
    "name": "glapsfun"
  },
  "repository": "https://github.com/glapsfun/sddx",
  "license": "MIT",
  "keywords": [
    "sdd",
    "tdd",
    "spec-driven",
    "worktree",
    "receipts",
    "oracle"
  ],
  "userConfig": {
    "workspace_mode": {
      "type": "string",
      "title": "Workspace mode",
      "default": "auto",
      "description": "Task workspace strategy: worktree | branch | auto"
    },
    "test_globs": {
      "type": "string",
      "title": "Test globs",
      "default": "",
      "description": "Space-separated extra globs classified as test files by the TDD gate"
    },
    "exempt_globs": {
      "type": "string",
      "title": "Exempt globs",
      "default": "",
      "description": "Space-separated extra globs exempt from the RED-phase write block"
    },
    "max_iterations_default": {
      "type": "number",
      "title": "Max iterations",
      "default": 5,
      "description": "Default stop rule: max loop iterations per task"
    },
    "board_enabled": {
      "type": "boolean",
      "title": "Board enabled",
      "default": true,
      "description": "Regenerate .sddx/BOARD.md automatically"
    },
    "oracle_runs_default": {
      "type": "number",
      "title": "Oracle runs",
      "default": 1,
      "description": "How many times verify executes the oracle; every run must pass (flakiness detection)"
    },
    "red_bash_allow": {
      "type": "string",
      "title": "RED Bash allow-list",
      "default": "",
      "description": "Space-separated extra commands the RED-phase Bash gate allows (extends the built-in list, never replaces it)"
    },
    "stuck_threshold": {
      "type": "number",
      "title": "Stuck threshold",
      "default": 3,
      "description": "Consecutive identical test failures before a task is flagged stuck and escalation is requested"
    },
    "pr_host": {
      "type": "string",
      "title": "PR host",
      "default": "",
      "description": "PR-host CLI for `sddx pr create`: gh | glab. Empty auto-detects from the origin remote"
    },
    "agent_model": {
      "type": "string",
      "title": "Agent model overrides",
      "default": "",
      "description": "Comma-separated role=model pairs (roles: orchestrator, planner, tddExecutor, verifier) — advisory, read by /sddx:run and /sddx:quick when dispatching subagents"
    },
    "prefer_solo": {
      "type": "boolean",
      "title": "Prefer solo",
      "default": false,
      "description": "Advisory hint read by /sddx:run to steer a single trivial task toward --solo / /sddx:quick"
    },
    "verbose": {
      "type": "boolean",
      "title": "Verbose CLI output",
      "default": false,
      "description": "When true, sddx config show also prints which source (env var, .sddx/config.json, or built-in default) resolved each key"
    }
  }
}
