# my
.help

# AI
.claude
.agents
AGENTS.md
CLAUDE.md
openspec

# deps
node_modules/

# npm pack output (release smoke test packs in the workspace)
*.tgz

# sddx examples (local runs only — CI uses its own scratch dir)
examples/*/.sandbox/

# sddx (generated) — do not edit between markers
.sddx/local/
.sddx/cache/
.sddx/sessions/
.sddx-worktrees/
# end sddx
