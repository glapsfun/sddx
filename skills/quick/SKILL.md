---
name: quick
description: Run a single sddx task through the PLAN → RED → GREEN → REFACTOR → VERIFY loop. Use when the user asks to execute one small, well-scoped task with spec-driven TDD.
---

# /sddx:quick (M0 stub)

The single-task sddx loop lands in M1. For now this skill only proves the
plugin loads and namespaces correctly.

If invoked: tell the user sddx is at milestone M0 (skeleton) and the quick
loop is not implemented yet. Do not attempt to emulate the loop.
